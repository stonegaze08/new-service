# Download binaries

```bash copy
# Download project binaries
mkdir -p $HOME/.lumera/cosmovisor/upgrades/v1.0.0/bin
wget -O $HOME/.lumera/cosmovisor/upgrades/v1.0.0/bin/lumerad https://snapshots.kjnodes.com/lumera-testnet/lumerad_v1.0.1-linux-amd64
chmod +x $HOME/.lumera/cosmovisor/upgrades/v1.0.0/bin/lumerad
```

<b>Thats it! Now when upgrade block height is reached, Cosmovisor will handle it automatically!</b>