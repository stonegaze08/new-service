import { Steps } from 'nextra/components'


<Steps>
# Cheat Sheet
A Tendermint cheat sheet is a quick reference for essential commands to control Tendermint blockchains.

## Managing keys

```js filename="Generate new key"
wardend keys add wallet
```

```js filename="Recover key"
wardend keys add wallet --recover
```
```js filename="List all key"
wardend keys list
```
```js filename="Delete key"
wardend keys delete wallet
```
```js filename="Export key"
wardend keys export wallet
```
```js filename="Import key"
wardend keys import wallet wallet.backup
```
```js filename="Query wallet balances"
wardend q bank balances $(wardend keys show wallet -a)
```

## Managing validators
```js filename="Create validator"
wardend tx staking create-validator \
--amount 1000000000000000000mpx \
--pubkey $(wardend tendermint show-validator) \
--moniker "your-moniker-name" \
--identity "your-keybase-id" \
--details "your-details" \
--website "your-website" \
--security-contact "your-email" \
--chain-id buenavista-1 \
--commission-rate 0.05 \
--commission-max-rate 0.20 \
--commission-max-change-rate 0.01 \
--min-self-delegation 1 \
--from wallet \
--gas-adjustment 1.5 \
--gas auto \
--gas-prices 10000000000000mpx \
-y
```

```js filename="Edit validator"
wardend tx staking edit-validator \
--new-moniker "your-moniker-name" \
--identity "your-keybase-id" \
--details "your-details" \
--website "your-website" \
--security-contact "your-email" \
--chain-id buenavista-1 \
--commission-rate 0.05 \
--from wallet \
--gas-adjustment 1.5 \
--gas auto \
--gas-prices 10000000000000mpx \
-y
```

```js filename="Unjail validator"
wardend tx slashing unjail --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```

```js filename="Validator jail reason"
wardend query slashing signing-info $(wardend tendermint show-validator)
```
```js filename="List active validator"
wardend q staking validators -oj --limit=3000 | jq '.validators[] | select(.status=="BOND_STATUS_BONDED")' | jq -r '(.tokens|tonumber/pow(10; 6)|floor|tostring) + " \t " + .description.moniker' | sort -gr | nl
```
```js filename="List incative validator"
wardend q staking validators -oj --limit=3000 | jq '.validators[] | select(.status=="BOND_STATUS_UNBONDED")' | jq -r '(.tokens|tonumber/pow(10; 6)|floor|tostring) + " \t " + .description.moniker' | sort -gr | nl
```
```js filename="View validator details"
wardend q staking validator $(wardend keys show wallet --bech val -a)
```

## Managing Tokens

```js filename="Withdraw reward from all validator"
wardend tx distribution withdraw-all-rewards --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Withdraw reward and commission"
wardend tx distribution withdraw-rewards $(wardend keys show wallet --bech val -a) --commission --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Delegate tokens to your validator"
wardend tx staking delegate $(wardend keys show wallet --bech val -a) 1000000000000000000mpx --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Delegate token to other validator, change <to-valoper-address> as you like"
wardend tx staking delegate <to-valoper-address> 1000000000000000000mpx --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Redelegate to another validator"
wardend tx staking redelegate $(wardend keys show wallet --bech val -a) <to-valoper-address> 1000000000000000000mpx --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Unbond token from your own validator"
wardend tx staking unbond $(wardend keys show wallet --bech val -a) 1000000000000000000mpx --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Send token to the wallet"
wardend tx bank send wallet <to-wallet-address> 1000000000000000000mpx --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
## Governance

```js filename="Query list proposal"
wardend query gov proposals
```
```js filename="View proposal by ID"
wardend query gov proposal 1
```
```js filename="Vote option yes"
wardend tx gov vote 1 yes --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Vote option no"
wardend tx gov vote 1 no --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Vote option asbtain"
wardend tx gov vote 1 abstain --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```
```js filename="Vote option NoWithVeto"
wardend tx gov vote 1 NoWithVeto --from wallet --chain-id buenavista-1 --gas-adjustment 1.5 --gas auto --gas-prices 10000000000000mpx -y
```

## Usefulness

```js filename="Update custom port"
CUSTOM_PORT=239
sed -i -e "s%^proxy_app = \"tcp://127.0.0.1:26658\"%proxy_app = \"tcp://127.0.0.1:${CUSTOM_PORT}58\"%; s%^laddr = \"tcp://127.0.0.1:26657\"%laddr = \"tcp://127.0.0.1:${CUSTOM_PORT}57\"%; s%^pprof_laddr = \"localhost:6060\"%pprof_laddr = \"localhost:${CUSTOM_PORT}60\"%; s%^laddr = \"tcp://0.0.0.0:26656\"%laddr = \"tcp://0.0.0.0:${CUSTOM_PORT}56\"%; s%^prometheus_listen_addr = \":26660\"%prometheus_listen_addr = \":${CUSTOM_PORT}66\"%" $HOME/.mineplex-chain/config/config.toml
sed -i -e "s%^address = \"tcp://0.0.0.0:1317\"%address = \"tcp://0.0.0.0:${CUSTOM_PORT}17\"%; s%^address = \":8080\"%address = \":${CUSTOM_PORT}80\"%; s%^address = \"0.0.0.0:9090\"%address = \"0.0.0.0:${CUSTOM_PORT}90\"%; s%^address = \"0.0.0.0:9091\"%address = \"0.0.0.0:${CUSTOM_PORT}91\"%" $HOME/.mineplex-chain/config/app.toml
```
```js filename="Disable indexer"
sed -i -e 's|^indexer *=.*|indexer = "null"|' $HOME/.mineplex-chain/config/config.toml
```
```js filename="Enable indexer"
sed -i -e 's|^indexer *=.*|indexer = "kv"|' $HOME/.mineplex-chain/config/config.toml
```
```js filename="Pruning update"
sed -i \
  -e 's|^pruning *=.*|pruning = "custom"|' \
  -e 's|^pruning-keep-recent *=.*|pruning-keep-recent = "100"|' \
  -e 's|^pruning-keep-every *=.*|pruning-keep-every = "0"|' \
  -e 's|^pruning-interval *=.*|pruning-interval = "19"|' \
  $HOME/.mineplex-chain/config/app.toml
```

## Maintenance

```js filename="Get validator information"
wardend status 2>&1 | jq .ValidatorInfo
```
```js filename="Get sync information"
wardend status 2>&1 | jq .SyncInfo
```
```js filename="Get node peer"
echo $(wardend tendermint show-node-id)'@'$(curl -s ifconfig.me)':'$(cat $HOME/.mineplex-chain/config/config.toml | sed -n '/Address to listen for incoming connection/{n;p;}' | sed 's/.*://; s/".*//')
```
```js filename="Check validator keys"
[[ $(wardend q staking validator $(wardend keys show wallet --bech val -a) -oj | jq -r .consensus_pubkey.key) = $(wardend status | jq -r .ValidatorInfo.PubKey.value) ]] && echo -e "\n\e[1m\e[32mTrue\e[0m\n" || echo -e "\n\e[1m\e[31mFalse\e[0m\n"
```
```js filename="Get live peers"
curl -sS http://localhost:23957/net_info | jq -r '.result.peers[] | "\(.node_info.id)@\(.remote_ip):\(.node_info.listen_addr)"' | awk -F ':' '{print $1":"$(NF)}'
```
```js filename="Configure minimum gas prices"
sed -i -e "s/^minimum-gas-prices *=.*/minimum-gas-prices = \"10mpx\"/" $HOME/.mineplex-chain/config/app.toml
```
```js filename="Enable prometheus"
sed -i -e "s/prometheus = false/prometheus = true/" $HOME/.mineplex-chain/config/config.toml
```
```js filename="Reset chain data"
wardend tendermint unsafe-reset-all --keep-addr-book --home $HOME/.mineplex-chain --keep-addr-book
```
```js filename="Remove node"
sudo systemctl stop wardend
sudo systemctl disable wardend
sudo rm -rf /etc/systemd/system/wardend.service
sudo rm $(which wardend)
sudo rm -rf $HOME/.warden
sed -i "/AIRCHAIN_/d" $HOME/.bash_profile
```
</Steps>